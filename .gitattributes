# 노트북은 커밋할 때 자동으로 출력을 지웁니다.
#   ★ 이 줄만으로는 동작하지 않습니다. 한 번은 아래를 실행해야 합니다.
#         bash scripts/setup_git.sh
#     filter 설정은 저장소가 아니라 로컬 git config 에 저장되기 때문입니다.
*.ipynb filter=nbstrip

# 노트북 diff 를 조금이라도 읽을 수 있게
*.ipynb -text
